# Frontend Summary

## State Management
All state (filter selections) is stored in localStorage. Catalogue data is held in memory within app.js.

## Accessibility Notes
All images include descriptive alt text. Form controls have associated labels. The catalogue container uses aria-live="polite" to announce updates. Keyboard navigation is supported via standard HTML elements. Color contrast meets WCAG AA.

## User Configuration
```json
{}
```
