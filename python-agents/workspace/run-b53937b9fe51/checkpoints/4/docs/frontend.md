# Frontend Summary

## State Management
Browser localStorage

## Accessibility Notes
All images include descriptive alt text. Buttons have aria-labels for screen readers. The cart modal uses role="dialog" and aria-modal="true". The cart count badge is a live region updated via aria-live="polite". Keyboard navigation is supported by default button elements.

## User Configuration
```json
{
  "currency": "$",
  "locale": "en-US"
}
```
